import styles from '../styles/projectmodal.module.scss';

interface ProjectModalProps {
	name: string,
}

export default function ProjectModal({name}: ProjectModalProps) {
	return (
		<div className={styles.modal}>
			
		</div>
	)
}